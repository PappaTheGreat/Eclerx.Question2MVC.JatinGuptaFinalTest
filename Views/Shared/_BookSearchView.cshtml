@model Eclerx.Question2MVC.JatinGupta.ViewModels.BookSearchVM

@if (Model.BookGenre != null)
{
    if (Model.BookGenre.Count() > 0)
    {
        <div class="col-6 mb-3">
            <table class="table table-striped table-hover">
                <tr>
                    <th>Book Name/th>
                    <th>Genre</th>
                    <th>Author Name</th>
                </tr>
                @foreach (var prd in Model.BookGenre)
                {
                    <tr>
                        <td>@prd.BookName</td>
                        <td>@prd.Genre</td>
                        <td>@prd.AuthorName</td>
                    </tr>
                }
            </table>
        </div>
    }
    else
    {
        <div class="h3">No Records</div>
    }
}
